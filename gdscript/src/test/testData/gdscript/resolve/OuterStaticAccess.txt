== Outer.gd ==
class_name Outer

static var STATIC_VAL := 1
var instance_val := 2
var names: Array[u][String[u]] = []

static func s() -> void:
	pass

func i() -> void:
	pass

class Sub:
    static func s_Sub(): pass
    var inst_Sub := 42

== Regular.gd ==
func _ready():
	Outer[r].s[r]()                  # static OK
	Outer[r].STATIC_VAL[r]           # static OK
	Outer[r].i[u]()                  # non-static should be filtered out by static context
	Outer[r].instance_val[u]         # non-static should be filtered out

	# Qualifier is `Outer` (for `Sub`) or `Outer.Sub` (for members)
	# When completing after `Outer.`, selecting `Sub` and then `Sub.` will set a class context.
	Outer[r].Sub[r].s_Sub[r]()          # static OK
	Outer[r].Sub[r].inst_Sub[u]         # non-static should be filtered out in static context