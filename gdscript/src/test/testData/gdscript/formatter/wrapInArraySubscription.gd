var returnData = {}
var email_address = ""
var company_name = ""


func _ready():
	if true:
		if false:
			if true:
				returnData['error_text'] = "User with  " + email_address + " email address is already part of " + company_name
