#brief Performs an SDF (signed-distance field) texture lookup within the visual shader graph.
#desc Translates to [code]texture_sdf(sdf_pos)[/code] in the shader language.
class_name VisualShaderNodeTextureSDF





